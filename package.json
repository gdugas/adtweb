{
  "name": "adtweb",
  "version": "0.0.1",
  "description": "Demo UI for DNS editing",
  "main": "index.js",
  "scripts": {
    "start": "npm run dev",
    "dev": "lite-server -c lite-config.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": {
    "email": "dugas.guillaume@gmail.com",
    "name": "Guillaume Dugas"
  },
  "license": "ISC",
  "dependencies": {
    "http-server": "^0.10.0"
  },
  "devDependencies": {
    "lite-server": "^2.3.0"
  }
}
